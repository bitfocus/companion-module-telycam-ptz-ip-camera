- 所有功能定义文件（如 actions, presets）均导出一个以 `Update` 开头的函数（如 `UpdateActions`），接收 `ModuleInstance` 实例作为参数来注册定义。
- VISCA 指令在代码中以十六进制字符串形式硬编码，并通过 `Buffer.from(hexString, 'hex')` 转换为二进制数据进行 UDP 发送。
- HTTP CGI 请求采用无状态 GET 方式，通过将 JSON 负载序列化后拼接到 URL 查询参数 `func=set` 或 `func=get` 中实现控制与查询。
